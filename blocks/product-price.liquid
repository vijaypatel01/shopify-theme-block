{{ block.settings.product.price | default: 1999 }}
{% schema %}
  {
    "name": "Product Price",
    "settings": [
      {
        "type": "product",
        "id": "product",
        "label": "Product"
      }
    ],
    "presets": [
      {
        "name": "Product Price",
        "settings": {
          "product": "{{ closest.product }}"
        }
      }
    ]
  }
{% endschema %}